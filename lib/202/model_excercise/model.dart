import 'package:flutter/material.dart';

class ModelExercise extends StatefulWidget {
  const ModelExercise({Key? key}) : super(key: key);

  @override
  _ModelExerciseState createState() => _ModelExerciseState();
}

class _ModelExerciseState extends State<ModelExercise> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
